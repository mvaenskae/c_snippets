/* Naive mergesort, first steps in writing proper code. The prototype adheres to
 ** the stdlibc qsort() prototype, but guarantees nothing else so far.
 */
void mergesort_top(void *const base, size_t nitems, size_t size, 
        int (*cmp)(const void *, const void *))
{
    // pointer to start
    // number of items
    // size of each item
    // comparator
    
    for (i
    printf("No bueno, doesn't work yet!\n");
}

void 


/* Naive mergesort, first steps in writing proper code. The prototype adheres to
 ** the stdlibc qsort() prototype, but guarantees nothing else so far.
 */
void mergesort_bottom(void *const base, size_t nitems, size_t size, 
        int (*cmp)(const void *, const void *))
{
    // pointer to start
    // number of items
    // size of each item
    // comparator
    
    printf("No bueno, doesn't work yet!\n");
}


/* Naive mergesort, first steps in writing proper code. The prototype adheres to
 ** the stdlibc qsort() prototype, but guarantees nothing else so far.
 */
void mergesort_natural(void *const base, size_t nitems, size_t size, 
        int (*cmp)(const void *, const void *))
{
    // pointer to start
    // number of items
    // size of each item
    // comparator
    
    printf("No bueno, doesn't work yet!\n");
}
